[answer6]

print(t.count()) # 行数
print(len(t.schema())) # 列数


----------------------------------------------

[tips]
・.count() メソッドを使うことでーブルの行数をカウントできる。
・.schema() メソッドを使うことでテーブルのデータ構造を確認することが
　でき、 len(t.schema()) とすることでカラム数（列数）をカウントできる。

----------------------------------------------

[参考] pandas記法

df.shape