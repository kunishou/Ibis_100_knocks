[answer23]

t[t.columns[2:10]]

----------------------------------------------

[Tips]
・特定カラム区間を抽出したい場合は、 t.columns で列名一覧を
　リストで取得し、スライスで抽出したいカラム区間を指定する。

----------------------------------------------

[参考] pandas記法

df.loc[:,'name':'cabin']

pandas では loc を用いることで カラム名で抽出区間を指定できる。